{
  "description": "macOS sandbox workarounds for Go TLS certificate verification",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bun ${CLAUDE_PLUGIN_ROOT}/hooks/sandbox.ts"
          }
        ]
      }
    ]
  }
}
