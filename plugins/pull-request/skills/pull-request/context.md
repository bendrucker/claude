# Pull Request Context

Before creating a pull request, gather the following context using shell commands:

- Current git status: `git status`
- Current git diff: `git diff --staged HEAD`
- Current branch: `git branch --show-current`
- Recent commits: `git log --oneline -20` (use this to determine commit style)
