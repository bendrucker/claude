---
type: llm
---
The body mentions both the deferred token-refresh rework and the unchanged retry behavior. Headings are optional: saying so in prose passes, and phrasing like "unchanged", "deferred to a follow-up", or "out of scope" is fine.

Fail if either is missing, or if either sits under a heading that names the document rather than a topic: `What I Didn't Do`, `What Changed`, `Notes`, `Other`. A topic heading such as `Deferred Work` or `Token Refresh` passes.
