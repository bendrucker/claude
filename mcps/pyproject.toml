[project]
name = "claude-code-mcps"
version = "0.1.0"
description = "MCP server configuration and management for Claude Code"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "awslabs.aws-documentation-mcp-server",
]

[tool.uv]
dev-dependencies = []