schema_version: "1.1"
name: config-move
tags: [holdout]
plugins: ["../.."]
execution:
  model: claude-sonnet-5
  max_turns: 20
  timeout_seconds: 300
  allowed_tools: [Read, Glob, Grep, Skill]
context:
  scaffold_script: fixture.sh
