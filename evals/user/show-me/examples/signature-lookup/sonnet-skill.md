`parseDuration` is defined in `src/duration.ts:4`:

```typescript
export function parseDuration(input: string, options: { allowNegative?: boolean } = {}): number
```

- `input: string` — the duration string to parse
- `options.allowNegative?: boolean` — optional, defaults to `{}`
- Returns `number`
