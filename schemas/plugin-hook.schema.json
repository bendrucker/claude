{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/bendrucker/claude/main/schemas/plugin-hook.schema.json",
  "title": "Claude Code Plugin Hooks Schema",
  "description": "Schema for Claude Code plugin hooks.json files",
  "type": "object",
  "properties": {
    "description": {
      "type": "string",
      "description": "Description of what the hooks do"
    },
    "hooks": {
      "$ref": "hook.schema.json"
    },
    "modules": {
      "type": "array",
      "description": "Function-hooks modules, relative to the hooks directory, each exporting register(on, options)",
      "items": { "type": "string" }
    }
  },
  "anyOf": [{ "required": ["hooks"] }, { "required": ["modules"] }],
  "additionalProperties": false
}
