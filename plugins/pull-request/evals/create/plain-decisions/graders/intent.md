---
type: llm
---
Judge only the PR body, the text after the `Title:` line. Ignore any commentary before that line, such as a note that the push or create command failed. The reply contains a PR body for a personal repo written as one to three prose paragraphs with no section headings or bulleted change list, stating each decision (per-plugin config over a shared package, the dropped verbose flag, the kept legacy path) as a claim about the code. It does not use instruction vocabulary ("on purpose", "deliberately", "worth noting") or describe which points it chose to include.
