{
  "mcpServers": {
    "terraform": {
      "command": "npx",
      "args": ["-y", "terraform-mcp-server@latest", "--toolsets=registry,terraform"],
      "env": {
        "TFE_TOKEN": "${TFE_TOKEN}"
      }
    }
  }
}
