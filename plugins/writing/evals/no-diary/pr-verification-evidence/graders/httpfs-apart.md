---
type: llm
---
The DuckDB httpfs observation is absent, or kept as a short aside set apart from the loader change rather than folded into it.
For example, a separate closing line such as "Note: DuckDB's httpfs cache is off by default." passes.
