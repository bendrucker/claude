---
type: llm
---
The body opens on the real cause (awaiting `spawn` before the stream drained) or on what the change does, never on the first theory.
For example, a body opening "Moves the `spawn` await below the stream drain, so the exit event no longer fires against a closed handle." passes.
