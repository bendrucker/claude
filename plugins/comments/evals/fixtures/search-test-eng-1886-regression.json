{
  "id": "search-test-eng-1886-regression",
  "path": "src/agents/whirl/integrations/salesforce/duckdb/search_test.py",
  "language": "python",
  "kind": "docstring",
  "comment": "\"\"\"Regression for ENG-1886: scope allowlist must come from the catalog.\n\n    A previous implementation derived the allowlist by querying\n    ``information_schema.tables`` for the ``config`` schema. That view\n    only lists DuckDB views actually bound on the current connection,\n    and ``search`` only preloads ``config.text_references`` before\n    validating scope. In prod, every other producer-declared scope was\n    therefore invisible to the validator and rejected as unknown\n    (``Unknown scope value(s) ['apexclass'] for search(): no config\n    table in this org has matching source_type.``), even though the\n    catalog knew about it. Dev fixtures bound every config view\n    eagerly, so the bug was invisible locally.\n\n    This test reproduces the prod state exactly: only\n    ``config.text_references`` is bound on the connection, but\n    ``apexclass`` is declared in the catalog. ``search(scope=[\"apexclass\"])``\n    must succeed. If anyone reintroduces ``information_schema.tables``\n    as the allowlist source, this test fails.\n    \"\"\"",
  "context": "475: async def test_scope_validates_against_catalog_under_lazy_materialization() -> None:\n476:     \"\"\"Regression for ENG-1886: scope allowlist must come from the catalog.\n477: \n478:     A previous implementation derived the allowlist by querying\n479:     ``information_schema.tables`` for the ``config`` schema. That view\n480:     only lists DuckDB views actually bound on the current connection,\n481:     and ``search`` only preloads ``config.text_references`` before\n482:     validating scope. In prod, every other producer-declared scope was\n483:     therefore invisible to the validator and rejected as unknown\n484:     (``Unknown scope value(s) ['apexclass'] for search(): no config\n485:     table in this org has matching source_type.``), even though the\n486:     catalog knew about it. Dev fixtures bound every config view\n487:     eagerly, so the bug was invisible locally.\n488: \n489:     This test reproduces the prod state exactly: only\n490:     ``config.text_references`` is bound on the connection, but\n491:     ``apexclass`` is declared in the catalog. ``search(scope=[\"apexclass\"])``\n492:     must succeed. If anyone reintroduces ``information_schema.tables``\n493:     as the allowlist source, this test fails.\n494:     \"\"\"",
  "action": "trim",
  "category": "docstring-scope",
  "trimTo": "\"\"\"Regression for ENG-1886: scope allowlist must come from the catalog.\n\n``information_schema.tables`` lists only the views bound on the current\nconnection, so it misses every config view ``search`` has not preloaded.\n\"\"\"",
  "trimToLines": null,
  "fact": [
    "allowlist must come from the catalog",
    "information_schema.tables"
  ],
  "source": "arya/!539 search_test.py:476",
  "note": "The contract line and the reason information_schema.tables misses unbound views are the facts a reader needs to avoid reintroducing the bug. The prod error text, the dev-fixture history, and the restatement of the test body are narration the test and its ticket already hold."
}
