---
fail: []
---
<rewrite>
## Quoting

Quote every shell argument with single quotes. Single quotes stop the shell expanding `$`, backticks, and `!` inside the argument, so apply them to any value that came from a user, a branch name, or a file path.
</rewrite>
