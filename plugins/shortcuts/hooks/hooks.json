{
  "description": "Gates open command to .shortcut files only",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "bun \"${CLAUDE_PLUGIN_ROOT}/hooks/open.ts\"",
            "if": "Bash(open:*)"
          }
        ]
      }
    ]
  }
}
